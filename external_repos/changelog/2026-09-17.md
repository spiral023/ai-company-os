# Update-Lauf 2026-09-17

[← Übersicht](README.md)

- **Regulärer Lauf:** 59 Repos geprüft · 48 geändert · 11 unverändert · 0 Fehler
- **Separater Lauf:** `mleibetseder/ai-llm-wiki` unter `external_knowledge/` aktualisiert
- **Neu geladen:** `ayghri/i-have-adhd` (siehe `external_repos/INDEX.md`, kein eigener Update-Lauf, daher hier nicht aufgeführt)
- **Hinweis:** `openai/codex` scheiterte beim ersten Pull-Versuch an Windows' 260-Zeichen-Pfadlimit (Snapshot-Testdateien unter `codex-rs/tui/src/bottom_pane/chat_composer/snapshots/`); nach Zurücksetzen des unvollständigen Zwischenstands (`git checkout -- .` + `git clean -fd`) und `git config core.longpaths true` lief der Pull danach fehlerfrei durch.
- **Index:** Mechanische Metadaten vollständig neu gescannt; 26 inhaltliche Repo-Zusammenfassungen in `external_repos/INDEX.md` angepasst, dazu der separat gepflegte `mleibetseder/ai-llm-wiki`-Eintrag

## Nur Wartung, ohne inhaltliche Substanz

Kein eigener Unterabschnitt, da ausschließlich automatisierte oder rein kosmetische Änderungen ohne Auswirkung auf Funktion, Skills oder Scope:

- `0xNyk/council-of-high-intelligence` (`f506e010 → 94f9a90d`, 4 Commits) — reine Star-History-Chart-Refreshes.
- `jakubkrehel/make-interfaces-feel-better` (`5a4076ac → 35545ea1`, 1 Commit) — einzeilige Beschreibungspräzisierung in der README.
- `Leonxlnx/taste-skill` (`72e29953 → e79ca9ec`, 4 Commits) — reine Sponsoren-README-Pflege (Fluxion AI neu, Kimi/Vercel in „Open Source Supporters" verschoben).
- `shadcn/improve` (`03369ee6 → cac56e1e`, 1 Commit) — nur `.github/FUNDING.yml` hinzugefügt.

## Repos mit inhaltlichen Änderungen

### addyosmani/agent-skills

- **Commits:** `5a5ea45e → be4e44a9` (73 Commits)
- **Änderungen:** Neuer Skill `constraint-driven-development` mit mitgeliefertem Referenz-„Floor-Guard" (harte Untergrenzen, gegen die ein Plan geprüft wird) und zugehörigem `/constraints`-Command — Skillzahl steigt von 24 auf 25, Commands von acht auf neun. Performance- und Security-Checklisten deutlich vertieft (Backend-/DB-Substanz, Rate-Limiting muss in einem geteilten Store zählen, destruktive Pfadoperationen brauchen Allowlist-Root + Tiefenbegrenzung + Owner-Check), plus SLOs/Error-Budgets in `shipping-and-launch` und Context Budget Management in `context-engineering`.
- **Index:** Angepasst — Skill-/Command-Zahlen auf 25/9 aktualisiert, `constraint-driven-development` und `/constraints` ergänzt, Vertiefungen bei Performance/Security/Shipping/Context-Engineering integriert.

### affaan-m/ECC

- **Commits:** `d8409a4b → dd6ee538` (2.460 Commits)
- **Änderungen:** ECC 2.2 bringt einen geführten Multi-Harness-Setup-Assistenten (`ecc-universal@2.2.1 setup` / `install --guided`), der neben Claude Code und Codex jetzt auch **Kimi Code** unterstützt und vor jeder Änderung alle Install-Scopes inventarisiert. Skillzahl steigt von 286 auf 292 (u.a. neuer Rails-Framework-Patterns-Skill). Bemerkenswert: sehr viele sicherheitsgetriebene Fixes rund um Windows-Installer/PowerShell-Enforcement, Settings-Race-Conditions und GateGuard-Scoping — plus ein Sponsorwechsel (Atlas Cloud zu „Past Sponsors" verschoben, SerpApi neu als Sponsor).
- **Index:** Angepasst — Skillzahl auf 292, Harness-Liste um Kimi Code ergänzt, Rails-Patterns-Skill und der neue geführte Multi-Harness-Setup-Assistent integriert.

### AgriciDaniel/claude-seo

- **Commits:** `09d37c7b → 92795530` (105 Commits)
- **Änderungen:** Zwei Releases (v2.3.0, v2.3.1). Bemerkenswert (sicherheitsrelevant): v2.3.0 schließt eine SSRF-Lücke, bei der ein per Umgebungsvariable gesetzter HTTP-Proxy (`HTTPS_PROXY=http://169.254.169.254:...`) jeden Audit in einen Cloud-Metadata-Read verwandeln konnte — der Proxy-Host wird jetzt selbst gegen die Blockliste geprüft. v2.3.1 stuft die fünf urteilsintensiven Agenten (`seo-content`, `seo-geo`, `seo-sxo`, `seo-cluster`, `seo-drift`) auf `model: opus`, die übrigen 13 bleiben auf Sonnet, und ergänzt „Keywords Everywhere" als kostenlose Backlinks-Fallback-Quelle, wenn Moz nicht konfiguriert ist.
- **Index:** Unverändert — Skill-/Agentenzahl (25/18) und Kernbeschreibung bleiben zutreffend; die Änderungen sind Sicherheits-Hardening und eine zusätzliche optionale Datenquelle, keine Scope-Änderung.

### alirezarezvani/claude-skills

- **Commits:** `98180daf → 19392f7a` (194 Commits)
- **Änderungen:** Skillzahl von 364 auf 388 (Domänenzahl 18 → 20), Agenten von 104 auf 118, Commands von 120 auf 150. Komplett neue Domäne „Agent Launcher" (6 Skills, u.a. `interview`, `stage-launch`, `grade-iterate`, `run-without-you`) zum Bauen/Launchen/Bewerten eigener Claude Managed Agents im eigenen Account. Dazu neue Skills `deep-learning-book` (Goodfellow/Bengio/Courville-Begleitskill), `spinning-up-deep-rl` (OpenAIs Spinning-Up-Wissensbasis), `linkedin` (mit im Code verankerten User-Agreement-§8.2-Refusals) und `swedish-mentor`.
- **Index:** Angepasst — alle Kennzahlen (388 Skills/118 Agenten/150 Commands/~823 Referenzdateien) aktualisiert und die neue Domäne „Agent Launcher" sowie die weiteren neuen Skills integriert.

### anthropics/claude-code

- **Commits:** `45bdfa96 → 68ac8bbf` (115 Commits)
- **Änderungen:** Bemerkenswert: Der neue Ordner `mods/` enthält erstmals echten TypeScript-Quellcode — die drei in die Claude-Code-Binary compilierten Built-in-Plugins `sec-default`, `diff` (das `/diff`-Fenster) und `telemetry` (`$.telemetry`), inklusive Tests, eigenem Test-Kit und den vollständigen, generierten Engine-Typdeklarationen (`claude-code.d.ts`, >10.000 Zeilen) — als „Early Access"-API zum Schreiben eigener Mods gegen interne Engine-Hooks. Daneben normale CHANGELOG-Einträge (2.1.274): Memory-Warnung, MCP-Timeout-/Reconnect-Fixes, `/rewind`-Hinweis bei korrupten Transkripten u.v.m.
- **Index:** Angepasst — die bisherige Aussage „enthält keinen CLI-Quellcode" ist jetzt nur noch für die eigentliche Engine korrekt; `mods/` mit den drei Built-in-Plugin-Quellen und der Early-Access-Hook-API wurde als neuer, bemerkenswerter Bestandteil ergänzt.

### anthropics/skills

- **Commits:** `3b3fad96 → 34040c9c` (3 Commits)
- **Änderungen:** Nur der `claude-api`-Skill und `frontend-design` wurden aktualisiert: neue Referenzdateien `admin-api.md` und `cost-optimization.md`, Managed-Agents-`auto`-Permission-Policy und `ant beta:sessions connect` dokumentiert, Modellreferenzen auf Claude Fable/Mythos 5.1 aktualisiert; `frontend-design` überarbeitet, um generische Design-Defaults zu vermeiden.
- **Index:** Unverändert — Umfang und Beschreibung des `claude-api`-Skills sowie der übrigen Beispiel-Skills bleiben zutreffend, es handelt sich um Vertiefung/Aktualisierung bestehender Inhalte, keine neue Skill-Kategorie.

### bmad-code-org/BMAD-METHOD

- **Commits:** `67d876f1 → 0a000534` (173 Commits)
- **Änderungen:** Größere Distributions- und Strukturumstellung: `src/bmm-skills/`+`src/core-skills/` (mit v6-Shims) sind einer flachen `skills/`-Struktur (aktuell 29 Skills, Jinja2-Template-Renderer, `bmad-checkpoint-preview`→`bmad-walkthrough` umbenannt) gewichen. Die Installation läuft nicht mehr primär über `npx bmad-method install`, sondern über die Skills-CLI (`npx skills add bmad-code-org/BMAD-METHOD`) oder eine neue Claude-/Codex-Plugin-Marketplace (`bmad-code-org/bmad-plugins`, aufgeteilt in `bmad-method`/`bmad-toolbox`). Die Doku zog von einer Astro-Website (`website/`, komplett entfernt) zu einem neuen `docs-site/` um; `bmad-build` bekam einstellbare Route-Auswahl/-Reporting und ein Review mit „Lever"/„Lens"-Sets.
- **Index:** Angepasst — der veraltete Installationsweg (`npx bmad-method install`, `src/bmm-skills`-Pfade, v6-Shims) wurde durch die neue Skills-CLI/Plugin-Marketplace-Route, die flache `skills/`-Struktur und die neuen Build/Review-Features ersetzt bzw. ergänzt.

### bradygaster/squad

- **Commits:** `4b32f7be → 2099faf5` (196 Commits)
- **Änderungen:** Releases v0.13.0/v0.13.1. Kern der Entwicklung ist die Härtung der in v0.12.0 eingeführten gh-aw-Integration (GitHub Agentic Workflows): Install-Reihenfolge, Router/Dispatch-Logik und Plan-Lifecycle wurden repariert (u.a. `/squad` schlägt jetzt laut statt still fehl, Owner-/Agent-Bindings werden validiert, Dispatch-Slots werden korrekt aus dem Root statt der Epic aufgefüllt), außerdem neue `squad health`-Diagnose, automatisiertes Squad-Bootstrap neuer Repos, evidenzbasierte/geplante Retrospektiven und ein fail-closed `squad-deps-worker`. Daneben Sicherheits-Fixes wie eine Scribe-Archivierung, die Zustand nicht mehr zerstören kann, und eine strikte Trennung von Squad-Produktassets und Team-State.
- **Index:** Angepasst — ergänzt um die parallele CI-native Automatisierungsschicht über GitHub Actions (`gh-aw`), die neben dem lokalen Ralph-Watch-Mode Planung, Dispatch, Review und Retrospektiven auf Issue-/PR-Ebene übernimmt; das Kernkonzept (persistente, benannte Agenten-Teams, Entscheidungsprotokoll) bleibt unverändert.

### coreyhaines31/marketingskills

- **Commits:** `3df87f97 → 5b2c0007` (77 Commits)
- **Änderungen:** Skill-Zahl wächst von 49 auf 50 durch den neuen `events`-Skill (Webinare, Konferenzen, Sponsoring, Speaking). Ein "Founding Marketing"-Batch reichert bestehende Skills mit neuen Referenzen an (u.a. MPTV/Aktivierungsmodelle in `onboarding`, SLC-Readiness-Gate in `launch`, Partnership-Taxonomie in `co-marketing`). `ai-seo` erreicht v2.5.0 mit ChatGPT-5.6-Format-Volatilitätsleitfaden. Bemerkenswert: Das Projekt führt ein bezahltes „Verified Partners"-Sponsoring-Programm ein (Converly, Ploy als erste zahlende Partner) — eine Kommerzialisierung eines bis dahin rein community-getriebenen, MIT-lizenzierten Skill-Katalogs, mit eigenem Regelwerk (`tools/PARTNERS.md`) zur Abgrenzung von den neutralen Kern-Skills.
- **Index:** Angepasst — Skill-Zahl auf 50 korrigiert, `events`-Skill in die Kanalliste aufgenommen und das neue Verified-Partners-Sponsoring als Ergänzung zum bisherigen Text eingewoben.

### davila7/claude-code-templates

- **Commits:** `5d313282 → 5b1070a1` (105 Commits)
- **Änderungen:** „Function Hooks" werden zu „Mods" umbenannt und ins Anthropic-Plugin-Layout überführt, dazu 14 neue Game-Mods. WebMCP V1 exponiert den Komponenten-Katalog als Read-only-Agent-Tools. Bemerkenswert: Die Dashboard-Website führt gesponserte Platzierungen ein („CCT Ads", hinter einem Feature-Flag `PUBLIC_ADS_ENABLED`) — eine Kommerzialisierung der bislang neutralen Katalog-Website, die bei der Bewertung der Objektivität von Empfehlungen/Rankings mitgedacht werden sollte. Überwiegend jedoch laufende Katalogpflege (neue Skills, Agent-Verbesserungen, Blogposts) plus generierte Trending-/Sterne-Daten.
- **Index:** Unverändert — das Kernkonzept (Komponenten-Katalog, npm-CLI, Attribution übernommener Quellen, Prüfbedürftigkeit einzelner Komponenten) ist von den Änderungen nicht betroffen; die Ads-Funktion ist optional/inert und noch nicht zentral für die Bewertung.

### diegosouzapw/OmniRoute

- **Commits:** `65e81158 → 02c663cd` (31 Commits)
- **Änderungen:** Der Großteil der Commits ist CI-/Release-Infrastruktur-Härtung (npm Trusted Publishing via OIDC, SBOM-Anhang an GitHub Releases, Docker-Bun-Image-Bump, diverse Test-/Pipeline-Fixes) rund um den Release v3.8.50. Inhaltlich schließt dieser Pull die Fertigstellung von v3.8.50 ab: Provider-Zahl steigt auf 352, dokumentierte Modell-IDs auf 1.312, Quota-aware Scheduling („Quota-Share") ist jetzt live statt Roadmap-Punkt, und die Modality Bridge deckt zusätzlich zu Vision jetzt auch Audio ab.
- **Index:** Angepasst — die im Text genannten Zahlen (Provider, MCP-Tools, Routing-Strategien, Testanzahl) waren bereits vor diesem Pull veraltet (250 vs. tatsächlich 351) und wurden nun auf den aktuellen Stand gebracht (>350 Provider, 110 MCP-Tools, 19 Routing-Strategien, >39.000 Tests, Quota-Share-Scheduling und Audio-Modality-Bridge ergänzt).

### DietrichGebert/ponytail

- **Commits:** `2ed6c52c → e3ba2aa6` (14 Commits)
- **Änderungen:** Release v4.10.0 fügt native Cursor-Hooks hinzu (`hooks.json`, echte `sessionStart`-Injektion statt nur einer statischen Regeldatei); `/ponytail lite|full|ultra|off` lässt sich in Cursor jetzt per Plain-Message umschalten. Daneben Dokumentations-Commits ("Built with Ponytail"-Sektion mit erster Referenzimplementierung Retriever, Trendshift-Badges).
- **Index:** Angepasst — der Cursor-Eintrag wurde von „nur statische Regeldatei" auf „jetzt auch echte Hooks" präzisiert, was einen Teil des zuvor im Text genannten Risikos (Setup je Host uneinheitlich) entschärft; Node.js-Anforderung um Cursor ergänzt.

### Egonex-AI/Understand-Anything

- **Commits:** `32944829 → 6df3065f` (79 Commits)
- **Änderungen:** Überwiegend Zuverlässigkeits-Härtung der inkrementellen Analyse (scoped Symbol-Evidence, Erkennung von Ruby/Python-Runtime-Property-Installern, erhaltene `tested_by`-Kanten für Swift/Rust/Ruby/PHP) plus Windows-PowerShell-Support für Update/Uninstall (`install.ps1 -Update/-Uninstall`). Der 7. Agent (`article-analyzer` für `/understand-knowledge`) wird in der README-Tabelle jetzt explizit mit Zuordnung zum Befehl dokumentiert.
- **Index:** Unverändert — der Index nennt bereits korrekt „sechs bis sieben spezialisierte Agenten" inklusive `article-analyzer`; die Änderungen sind Zuverlässigkeits-Fixes ohne Scope-Änderung.

### emilkowalski/skills

- **Commits:** `d23d7f88 → 85e8e236` (2 Commits)
- **Änderungen:** Neuer Skill `mobile-native` (CSS-/Meta-Tag-Fixes, die eine Web-App auf dem Handy nativ statt „wie im Browser" wirken lassen: Sticky-Hover, Tap-Highlights, 100vh-Bug, Safe-Areas, Pull-to-Refresh) plus begleitendes `performance-cheatsheet.md`. Zweiter Commit entfernt eine Erwähnung eines (kommerziellen) Kurses aus allen Skill-Dateien.
- **Index:** Angepasst — Skill-Zahl von „zwölf" auf 13 korrigiert, `mobile-native` in die Liste und die Plattform-Erweiterung (Web/Mobile/Apple) aufgenommen.

### farion1231/cc-switch

- **Commits:** `5ca9459d → 06082e18` (113 Commits)
- **Änderungen:** Drei Releases (v3.20.1–v3.20.3) plus unveröffentlichte Folge-Commits. Wichtigster Fund: Codex CLI 0.149 brach mit einem Breaking Change (Drittanbieter-Provider erben keine Ambient-Credentials mehr aus `auth.json`) alle bestehenden Drittanbieter-Switches; cc-switch reagiert mit einem Umbau auf Config-only-Provider-Switching (Key wandert in die `config.toml`-Providertabelle, `auth.json` bleibt reines ChatGPT-Login) samt Reparatur zahlreicher Legacy-Config-Formen. MiniMax Code (mcode) wird als neuntes verwaltetes Tool integriert (Provider/Modell-Verwaltung, MCP, Skills, Session-Import). Bemerkenswert: Der bisherige Sponsor Atlas Cloud verliert offengelegt seinen Sponsorenstatus (Presets bleiben, Sponsor-Kennzeichnung entfällt), während zwei neue zahlende Sponsoren (9527CODE, SoleAPI) aufgenommen werden.
- **Index:** Angepasst — Tool-Zahl von acht auf neun korrigiert (MiniMax Code ergänzt) und der Codex-0.149-Breaking-Change samt Config-only-Gegenmaßnahme in den Text eingewoben, da dies eine für die Kernfunktion (Provider-Switching) relevante Architekturänderung ist.

### garrytan/gstack

- **Commits:** `85fd9db5 → a6b3a575` (21 Commits)
- **Änderungen:** v1.69.0.0 bis v1.87.4.0: Aside (ein eigener Browser) wird zum von gstack primär gesteuerten Browser für jeden Browsing-Skill, den PDF/Diagramm-Renderer und Web-Recherche — der gebündelte Browser bleibt nur noch automatischer Fallback (v1.81.0.0). Codex und Claude Code werden standardmäßig auf Frontier-Modelle gesetzt (v1.84.1.0), Outside-Reviews werden jetzt per Harness geroutet (v1.86.0.0), und verifizierte CSO-Audits samt replayfähigen Repair-Bundles kommen hinzu (v1.87.0.0). Zuletzt: Review-Evidenz wird an den tatsächlich geprüften Tree gebunden und Health-Failures samt Coverage werden offengelegt statt verschluckt (v1.87.3.0/v1.87.4.0).
- **Index:** Unverändert — der Workflow-Zyklus, GBrain und die Sicherheitsmechanismen bleiben exakt beschrieben; die Aside-Browser-Umstellung ist eine Implementierungsdetail-Verschiebung innerhalb der bereits erwähnten Browser-QA-Fähigkeit.

### github/spec-kit

- **Commits:** `27f50f7e → 5e952140` (140 Commits)
- **Änderungen:** Versionssprung v1.0.1 (21.08.2026) auf v1.0.8 (17.09.2026): neue Coding-Agent-Integrationen für Docker Agent, DeepSeek Harness (DSH) und Muse Code; `specify artifact`-Introspektion und artefakt-eigene Contribution-Lookups; Workflow-Slots sowie JSON-Output für Preset-/Extension-Listen. Mehrere sicherheitsrelevante Härtungen am Extension-/Katalogsystem: `catalog-latest-url-bypass` erfordert jetzt tag-gepinnte Katalog-Download-URLs, Release-Asset-Metadaten-URLs werden validiert, und Aliase dürfen keine Core-Commands mehr überschatten.
- **Index:** Unverändert — Spec-Kit bleibt bei kontinuierlicher Patch-Entwicklung (1.0.x) innerhalb des bereits beschriebenen Extension-/Preset-/Workflow-Systems; die neuen Integrationen und Workflow-Slots sind Erweiterungen des bestehenden Scopes, kein Rahmenwechsel.

### Graphify-Labs/graphify

- **Commits:** `b2cd3626 → 26b02b5e` (290 Commits)
- **Änderungen:** Bis Version 0.9.63: Robot Framework (`.robot`/`.resource`) wird als neuer optionaler Extractor unterstützt, Terraform-Modulaufrufe mit lokalem `source`-Pfad lösen jetzt zu einem Modul-Knoten auf (Caller→Implementierung-Topologie sichtbar), und Python 3.14 wird unterstützt (inkl. Ersatz vulnerabler Abhängigkeiten). Daneben zahlreiche Cross-File-Resolver-Fixes (Elixir `alias`/`use`, Rust `self.method()` über `impl`-Blöcke, Ruby-Vererbungsketten) und ein Performance-Fix (Python-Symbolauflösung ~47 % schneller).
- **Index:** Unverändert — Zweck, Artefakte und die Einordnung als Quelle des lokal installierten `graphify`-Skills bleiben exakt gültig; die Änderungen sind überwiegend Extraktor-Robustheit und inkrementelle Sprachabdeckung, keine Scope-Änderung.

### headroomlabs-ai/headroom

- **Commits:** `34a55175 → b8b222f9` (120 Commits)
- **Änderungen:** Neues `headroom deploy` (turnkey lokales Deployment + Agent-Konfiguration) als weiterer Einstiegsmodus; Version 0.37.0 bringt session-bewussten `/v1/compress`-Sidecar-Modus und vereinheitlicht Proxy- und Sidecar-Kompression auf einer Session-Engine. Bemerkenswert: mehrere sicherheitsrelevante Fixes — ein Pfad-Parameter-SSRF in der Vertex-Location-Validierung wurde geschlossen, `HEADROOM_PROXY_TOKEN` wird jetzt auch bei WebSocket-Handshakes erzwungen, und der Bearer-Auth-Scheme-Vergleich ist nun case-insensitiv (RFC 7235).
- **Index:** Unverändert — Library/Proxy/MCP/Agent-Wrapper-Beschreibung und `headroom learn` bleiben gültig; die Änderungen sind Sicherheits-Hardening und ein zusätzlicher Deployment-Modus innerhalb des bestehenden Konzepts.

### juliusbrussee/caveman

- **Commits:** `7bb71309 → 542442ba` (295 Commits)
- **Änderungen:** Native Agent-Wraps wachsen von 8 auf 10 (neu: Kilo Code, Qwen Code, Pi); ein Firefox-Port der Caveman-Mode-Browser-Extension kommt hinzu (#810); und eine neue „hosted mode"-Proxy-Variante mit Inbound-Token, AWS-Credential-Chain sowie Container-/Deploy-Manifesten sowie ein separates Framework-Middleware-Paket (Adapter, SDK-Runtime, Proxy-Routes, Conformance-Harness) für Drittanbieter-Frameworks werden eingeführt.
- **Index:** Unverändert — der Split-Lizenz-Hinweis der README (MIT für Skill/CLI/SDKs, BSL-1.1 für Engine/Proxy/MCP) bestand bereits vor diesem Update, ebenso die Kategorien Skill/Proxy/cavecrew-Subagenten; die neuen Wrap-Ziele und die Hosted-Proxy-Variante sind Erweiterungen des bereits beschriebenen Proxy-/Multi-Agent-Konzepts.

### kepano/obsidian-skills

- **Commits:** `a1dc48e6 → 3ccff533` (3 Commits)
- **Änderungen:** Ein sechster Skill `knap` kommt hinzu — rendert Markdown aus Templates und strukturierten JSON-/CSV-Daten über die Knap-CLI, u.a. um Defuddle-Output in ein Notizformat zu bringen; daneben eine gekürzte Beschreibung des `defuddle`-Skills und kleinere README-Anpassungen.
- **Index:** Angepasst — der Fließtext sprach von „fünf klar getrennten Skills" (die Struktur-Zeile war bereits vom Script auf „6× SKILL.md" aktualisiert, der Prosa-Absatz aber nicht); auf sechs Skills korrigiert und `knap` ergänzt.

### langchain-ai/openwiki

- **Commits:** `a525ed88 → 715109a8` (102 Commits)
- **Änderungen:** Coding-Agent-Integrationen wachsen von zwei (Codex, Claude Code) auf sechs (zusätzlich IBM Bob, OpenCode, Cursor, Kiro), gesteuert über einen neuen MCP-Lifecycle (`openwiki_begin` … `openwiki_finish`), der nur noch sparsame Claim-Entscheidungen pro Seite verlangt. Die Repository-Generierung wurde von einer kompletten Neugenerierung auf eine wiederaufnehmbare Seiten-Job-Pipeline mit durablem Run-Status (`openwiki/.run.json`) umgestellt, die nach Unterbrechung fortsetzt statt neu zu starten; zusätzlich Gemini-Reasoning-Effort-Mapping.
- **Index:** Angepasst — die Integrationsliste auf die sechs aktuellen Ziele samt MCP-Lifecycle erweitert und die Neugenerierungs-Beschreibung durch die wiederaufnehmbare Seiten-Job-Pipeline ersetzt.

### mattpocock/skills

- **Commits:** `5b15a47f → 74ca5fe0` (17 Commits)
- **Änderungen:** Zwei neue Skills im `in-progress`-Bereich: `retro` leitet nach einer Coding-Session eine strukturierte Retrospektive an (Kategorien Navigation, Automated Checks, Coding Standards, Global AGENTS.md, Tool Economy, No-ops, Information Access) und schiebt mechanische Coding-Standard-Verstöße konsequent Richtung deterministischer Checks statt neuer Prosa-Regeln; `pr` liefert eine PR-Body-Vorlage mit Vorher/Nachher-Evidenz-Paaren und einer expliziten Merge-Risiko-Einschätzung (One-way/Two-way-Door, Blast Radius).
- **Index:** Angepasst — die beiden neuen in-progress-Skills `retro` und `pr` sind ergänzt.

### microsoft/markitdown

- **Commits:** `9dc0d657 → 945314a4` (88 Commits)
- **Änderungen:** Breaking API-Rename: `result.text_content` heißt jetzt `result.markdown`. Dutzende Format-Fixes an DOCX/PPTX/RSS/CSV/EPUB/ZIP/Outlook-MSG-Konvertern, ein Refactoring der Office-OCR-Konverter auf gemeinsame Kern-Pipelines, Migration auf MCP SDK 2.x sowie eine Sicherheits-Härtung, die UNC- und Windows-Device-Pfade jetzt zurückweist. Bemerkenswert: Eine neue „What to Contribute"-Sektion grenzt den Projekt-Scope explizit ab — Webserver, REST-APIs, Web-Frontends und Desktop-Apps werden ausdrücklich nicht mehr als Beiträge akzeptiert.
- **Index:** Unverändert — die Scope-Klärung bestätigt nur die bestehende Beschreibung (CLI/Python-API/Docker/MCP-Server/Plugin-System), keine neuen Formate oder Architekturänderungen.

### millionco/react-doctor

- **Commits:** `bd084063 → 499a0208` (50 Commits)
- **Änderungen:** Schwerpunkt liegt auf Performance und Rauschreduktion statt neuen Features: ein persistenter Oxlint-Worker-Pool mit Parent-Side-Overlap bringt laut Benchmark 2,4–5,5× Speedup auf dem Large-Repo-Corpus, TypeScript-/Git-/Worker-Boot sind jetzt vom kritischen Scan-Pfad entkoppelt. Mehrere gering wertige Regeln wurden zurückgezogen und Cleanup-Checks auf Opt-in gestellt, dazu vier weitere Regel-Fehlalarme stumm geschaltet und die CLI akzeptiert jetzt positionale Dateipfade.
- **Index:** Unverändert — reine Performance-/Rauschreduktions-Pflege ohne neue Regel-Kategorien oder Architekturänderung.

### msitarzewski/agency-agents

- **Commits:** `ebe9c99a → ad9264e3` (21 Commits)
- **Änderungen:** Sechs neue Engineering-Spezialisten sind gelandet: China Network Engineer, Platform Engineer, PDF Engine Architect, ATS Validator Architect und Universal Document Compiler; dazu Knowledge Graph Engineer und Master Plan Architect kurz zuvor. Bemerkenswert: Erstmals entsteht eine komplett neue, 18. Division „Research" mit dem Agenten Research Synthesist für Literatur-/Quellensynthese — bisher deckten alle Divisionen operative Fachgebiete ab, dies ist die erste rein methodische.
- **Index:** Angepasst — Division-Zahl auf 18 korrigiert, Agentenzahl auf rund 260 angehoben, die neuen Spezialisten sowie die neue Research Division mit Research Synthesist sind ergänzt.

### nextlevelbuilder/ui-ux-pro-max-skill

- **Commits:** `bc826e22 → 15de38fb` (42 Commits)
- **Änderungen:** Design-Systeme werden jetzt in einem echten Ordner pro Projekt abgelegt (`design-system/<slug>/MASTER.md` statt einer flachen, projektübergreifend geteilten `MASTER.md`) — macht die bereits beworbene „projektübergreifende Persistenz" erst wirklich mehrprojektfähig. Neue Logo-Provider Atlas Cloud und MuAPI, ein `--dry-run`-Flag für `init` sowie drei neue README-Übersetzungen. Bemerkenswert: Das README bewirbt jetzt aktiv das kommerzielle Schwesterprojekt „3Dviz Pro Max" eines Drittautors als eigenen Werbeblock.
- **Index:** Unverändert — die Ordnerstruktur-Korrektur präzisiert nur die bereits beschriebene Persistenzfunktion, keine neue Kernfunktion.

### NVIDIA/SkillSpector

- **Commits:** `698e2bf2 → c3bb8132` (117 Commits)
- **Änderungen:** Die Musterzahl wächst von 70 auf 71 durch EA5 (External Model or Provider Selection), das Modell-/Provider-Pins oder Coding-CLI-Shell-Outs erkennt, die das Abrechnungskonto wechseln können. Vier neue LLM-Provider kommen hinzu: Ollama, Azure OpenAI, ein generischer OpenAI-kompatibler Endpunkt sowie Gemini CLI und OpenCode CLI. Neue strikte CLI-Gates `--fail-on-findings`/`--fail-on-incomplete` lassen den Exit-Code direkt auf Findings bzw. unvollständige Abdeckung reagieren, dazu eine neue OpenCode-Extension, die SkillSpector als `/skillspector`-Tool direkt in Agenten-Sessions einbindet.
- **Index:** Angepasst — Musterzahl auf 71 korrigiert (inkl. EA5), die neuen Provider, die strikten Fail-Gates und die OpenCode-Extension sind ergänzt.

### open-gsd/gsd-core

- **Commits:** `1178c5f9 → bff99a8b` (505 Commits, drei Minor-Releases 1.12.0–1.14.0)
- **Änderungen:** Neuer `workflow.compact_content`-Modus splittet Workflows in eine knappe Spine- und eine ausgelagerte Detail-Datei — ein Gegenzug zum wachsenden Token-/Kontextverbrauch des Frameworks. `/gsd:code-review` kann sein internes Review jetzt optional durch registrierte externe Reviewer-Lanes gegenprüfen lassen. Der Großteil der 505 Commits ist Integritäts-Hardening rund um Phasen-/Milestone-Tracking.
- **Index:** Angepasst — der neue `compact_content`-Token-Sparmodus und die externen Reviewer-Lanes bei `/gsd:code-review` sind ergänzt.

### openai/codex

- **Commits:** `c9b19deb → ea218f5c` (1.219 Commits)
- **Änderungen:** 1.219 Commits bringen mehrere neue Crates: `worktree` für native Git-Worktree-Verwaltung mit TUI-Browser, `windows-sandbox-service` als registrierter Windows-Hintergrunddienst mit eigener Paketverwaltung, ACLs und Provisioning. Bemerkenswert: neue Crates `voice-host` und `realtime-webrtc` für Echtzeit-Sprachinteraktion — ein ungewöhnlicher Schritt für ein reines CLI-Coding-Tool — dazu `agent-roles`, `attachment-store`, `guardian-context` und `user-verification` für Rollen-, Anhang- und Verifikationslogik.
- **Index:** Angepasst — der veraltete Absatz über „Guardian"-Threads/typisierte Inhaltsarten (Stand August) wurde um die neuen September-Crates ersetzt (Worktree, Windows-Sandbox-Dienst, Voice/WebRTC, Rollen/Anhänge/Verifikation).

### OthmanAdi/planning-with-files

- **Commits:** `9e94390e → 2fbbd77b` (90 Commits)
- **Änderungen:** 90 Commits über sechs Releases (v3.16.1 bis v3.20.0). Schwerpunkte: DeepSeek Harness (DSH) wird über ein natives Cordis-Plugin zum First-Class-Host; Cursors native PowerShell-Hooks lösen jetzt benannte Pläne auf; ein Security-Fix verhindert, dass `.planning/.active_plan` per Shell-Redirection statt atomar geschrieben wird. Bemerkenswert: v3.17.0 behebt eine Performance-Regression, bei der jeder Claude-Code-Hook unter Windows rund 130 Prozesse forkte und das 10-Sekunden-Hook-Timeout riss (Fix: Python-Fastpath, 8s auf 0,3s gesenkt).
- **Index:** Angepasst — Versionsstand von v3.11 auf v3.20 aktualisiert, DeepSeek Harness (DSH) als fünfter First-Class-Host neben Claude Code, Pi, Hermes und OpenCode ergänzt.

### paperclipai/paperclip

- **Commits:** `16b59c93 → 84fe8990` (657 Commits)
- **Änderungen:** 657 Commits, u.a. der neue CLI-Befehl `test-drive` für isolierte, sofort mit CEO-Agent vorinitialisierte Testinstanzen (Harness wählbar), optionales Sentry-Error-Monitoring, neue Integrationen (CreateOS-Sandbox-Provider, Railway-Deployment, you.com-Suche). Bemerkenswert: durchgängige Umbenennung von „AI companies" zu „AI organizations" in README und UI — ein Branding-Wechsel, der auf bewusste Abgrenzung von wörtlichen Firmen-Behauptungen hindeutet.
- **Index:** Unverändert — Architektur (Org-Charts, Budgets, Governance, Multi-Tenant-Isolation) und Risikoeinschätzung bleiben zutreffend; die deutsche Zusammenfassung verwendet bereits „Organisation" statt „Company".

### pbakaus/impeccable

- **Commits:** `56f44523 → f2c70518` (328 Commits)
- **Änderungen:** 328 Commits erweitern den Skill von 23 auf 24 Befehle (neu: `/impeccable generate` für automatische Element-Varianten im Live-Browser) und von 59 auf 61 deterministische Detektor-Regeln. `PRODUCT.md` trennt jetzt klar dauerhafte Produktfakten von oberflächenbezogener visueller Richtung (`DESIGN.md`); neue Provider-Unterstützung für Hermes Agent, Veto und eine VS-Code-Marketplace-Extension für GitHub Copilot.
- **Index:** Angepasst — Befehlszahl (23→24) und Regelzahl (59→61) aktualisiert, PRODUCT.md-Beschreibung präzisiert, neue Provider (Hermes Agent, Veto) ergänzt.

### petergyang/human-review

- **Commits:** `ba64d116 → f9a55812` (1 Commit)
- **Änderungen:** Ein Commit (v0.8.2) behebt, dass Plain-Clicks auf Tabs/Toggles/Accordions im Edit-Modus verschluckt wurden — Buttons mit `aria-expanded`, `aria-controls`, `aria-pressed`, `aria-selected` oder `role="tab"` erhalten jetzt normale Klicks, bleiben aber weiter textbearbeitbar; reine Buttons/Links benötigen weiterhin ⌘-Click.
- **Index:** Unverändert — reiner Bugfix ohne Scope-Änderung.

### router-for-me/CLIProxyAPI

- **Commits:** `a7e3596b → b773607e` (423 Commits)
- **Änderungen:** 423 Commits, im Kern die komplette Neuintegration von Devin (Cognition AI) als Provider — OAuth-Flow, Modell-Fetching, Tool-Call-Aggregation, Rate-Limit-Klassifizierung (rund 69 der 423 Commits betreffen Devin, das vorher gar nicht existierte). Dazu Übersetzer-Ausbau für Reasoning-Felder, Cache-Token-Accounting und Tool-Namens-Kollisionen über mehrere Provider sowie Management-API-Pagination für Auth-Dateien.
- **Index:** Angepasst — Devin als neuer unterstützter Provider (API-Kompatibilität und OAuth-Login) in Provider-Liste und OAuth-Aufzählung ergänzt.

### rtk-ai/rtk

- **Commits:** `29f9bb71 → 5e0f92cd` (529 Commits)
- **Änderungen:** 529 Commits über die Releases 0.46.0–0.48.0: neue Laufzeitunterstützung für Bun und Deno, `rtk mvnd` für Maven Daemon, ein PHP-`.phpt`-Filter (laut Changelog −99 %) sowie ein Windows-Fix für falsch decodierte Konsolenausgaben. Bemerkenswert: Breaking Change in v0.47.0 — `rtk grep --file-type`/`-t` wurde entfernt, `-l`/`-m` unter `rtk grep` sind jetzt die nativen grep-Flags statt rtks eigener `--max-len`/`--max`.
- **Index:** Unverändert — Zweck, unterstützte Agenten und die dokumentierte Grenze (Read/Grep/Glob-Tools passieren den Bash-Hook nicht) bleiben zutreffend; die neue Runtime-/Paketmanager-Unterstützung fällt unter die bereits genannten generischen Kategorien.

### shadcn-ui/ui

- **Commits:** `ac60ef5c → a87a63b2` (53 Commits)
- **Änderungen:** Mehrere Registry-Erweiterungen sowie neue Registry-Health-Monitoring-Infrastruktur und OIDC-Support für private GitHub-Registries. Größerer interner Umbau: CLI, Registry und Templates wechseln von `tailwind-merge` auf das neue interne `@shadcn/helpers`-Utility-Paket `cn`; der `shadcn`-Skill selbst bekommt eine kleine neue Chat-Regel und importiert jetzt `cn` statt `@/lib/utils`. Rest überwiegend Dependabot-Bumps.
- **Index:** Unverändert — die Registry-/OIDC-Erweiterungen und die `cn`-Migration sind interne Tooling-Änderungen ohne Auswirkung auf den beschriebenen Scope der beiden Skills (`shadcn`, `migrate-radix-to-base`).

### shanraisshan/claude-code-best-practice

- **Commits:** `d57ddc78 → 4b0c0c45` (297 Commits)
- **Änderungen:** Praktisch der gesamte Commit-Verlauf sind automatisierte tägliche Drift-Checks und Badge-/Sternzahl-Refreshs (v2.1.263 → v2.1.274). Wenige echte Inhaltsänderungen darunter: `/rate-limit-options`-Command ergänzt (Count 91→92), `/output-style`-Command ergänzt (93→94), neues `omitClaudeMd`-Feld für Subagenten dokumentiert, und `skill-doctor` zur Skill-Liste hinzugefügt.
- **Index:** Unverändert — reine Drift-/Badge-Pflege, keine Scope-Änderung der beschriebenen Wissensbasis.

### teng-lin/notebooklm-py

- **Commits:** `3bb0c185 → 5472d7cf` (317 Commits)
- **Änderungen:** Release v0.8.2 bringt ein komplett neues Android-Backend (native gRPC-Mobil-API statt Web-Cookies, Auth über einen dauerhaften Master-Token) als Alternative zum Web-Backend. Die anschließende 0.9-Entwicklung baut die öffentliche API grundlegend um (typisierte `ClientConfig`, `outcomes`-Modul mit Commit-/Recovery-Evidenz statt Silent-Retry) und schließt eine Pfadtraversal-Lücke: MCP-Stdio-`source_add` mit Host-Pfaden ist jetzt standardmäßig verweigert. Bemerkenswert: Der neue Master-Token für das Android-Backend ist laut eigenem SECURITY.md „account-equivalent" — ein deutlich mächtigeres Credential als die bisherige Cookie-Momentaufnahme des Web-Backends.
- **Index:** Angepasst — Android-Backend, 0.9-API-Umbau (`ClientConfig`/`outcomes`) und die MCP-Stdio-Sicherheitslücke sind in die Zusammenfassung eingeflossen.

### thedotmack/claude-mem

- **Commits:** `e2d1df56 → d8bc9755` (304 Commits)
- **Änderungen:** Zwei neue Integrationen: ein Telegram-Bot mit kurzen Session-Wrap-up-Benachrichtigungen aus der Stop-Zusammenfassung, und ein Grok-Bot, der ein wachsendes Memory-INDEX live aus dem Worker mitschreibt. Sicherheitsverschärfung: Observer-Sessions dürfen `SendMessage` und `ListAgents` nicht mehr aufrufen. Windows-Fix: Der Daemon startet Worker nicht mehr mit dem Nutzer-Projektverzeichnis als cwd, sondern mit DATA_DIR (behebt Ordner-Locks); zudem CVE-2026-13311-Fix (shell-quote-Upgrade).
- **Index:** Angepasst — die Telegram- und Grok-Bot-Integrationen sowie die Observer-Rechteverschärfung und der Windows-cwd-Fix sind ergänzt.

### upstash/context7

- **Commits:** `c3248289 → dedb03d5` (39 Commits)
- **Änderungen:** MCP-Server erhält native OpenTelemetry-Observability; neue Dokumentation für natives AWS-Bedrock-Setup inkl. regionaler Konfiguration; weitere CLI-Setup-Ziele (GitHub Copilot CLI, Devin, VS Code, On-Premise-Betrieb); Docs7-Attribution dokumentiert und aktiviert; SDK-HTTP-Schicht umfassend neu geschrieben.
- **Index:** Angepasst — OpenTelemetry-Observability, natives AWS-Bedrock-Setup und Docs7-Attribution sind in die „Neu"-Aufzählung ergänzt.

### vercel-labs/agent-skills

- **Commits:** `dd089a8c → 063bee94` (11 Commits)
- **Änderungen:** Reine Dokumentationsverfeinerung des `react-view-transitions`-Skills: Suspense-Crossfade-Host-Platzierung dokumentiert, eine neue Troubleshooting-Referenzdatei ergänzt, und eine Inkonsistenz bei der nested-View-Transition-Regel zwischen `SKILL.md` und `nextjs.md` behoben.
- **Index:** Unverändert — betrifft nur die Doku-Tiefe eines bereits gelisteten Skills, kein neuer Scope.

### vercel-labs/skills

- **Commits:** `435076e7 → 7407f389` (46 Commits)
- **Änderungen:** Bis v1.7.0: neue Notion-Skills-Integration (v1.6.0), Azure-Repos-Git-URLs als weitere unterstützte Quelle, Pinnen von Skills auf einen Commit-SHA, sowie zwei neue Agenten (Sarvam Code, fx — Agentenzahl 73→75).
- **Index:** Angepasst — Notion-Integration, Azure Repos, Commit-SHA-Pinning und die neue Agentenzahl (75 statt 73) sind ergänzt.

### wshobson/agents

- **Commits:** `2b49247f → 4236bb91` (19 Commits)
- **Änderungen:** Pi (pi.dev) wird als sechstes generiertes Harness-Ziel ergänzt — zuvor fünf Ziele neben Claude Code als Quelle der Wahrheit. Neues `superself`-Plugin und ein externer Marketplace-Eintrag für „HOL Guard"; neue MLOps-Lab-CI-Pipeline (W&B + Hugging Face).
- **Index:** Angepasst — sechs statt fünf Harness-Ziele (neu: Pi/pi.dev) sowie das `superself`-Plugin sind ergänzt; Plugin-/Skill-/Agenten-/Command-Zahlen (92/202/181/105) bleiben laut Nachzählung im Repo unverändert korrekt.

### mleibetseder/ai-llm-wiki (separat unter `external_knowledge/`)

- **Commits:** `0761bb3 → 337cee1` (13 Commits)
- **Änderungen:** Neu sind vor allem agentische Design-Patterns (Google-Cloud-Referenz plus „Awesome Agentic Patterns"), Loop Engineering nach Addy Osmani, Context Engineering nach Anthropic/Kushal Banda samt SKILL.state-Paper, der „Practical Guide to Building Agents" von OpenAI, Manager-/Worker-Agent-Architekturen, der Langfuse/OpenTelemetry-Observability-Stack, Task-Scoped Memory Contracts, ein LLMOps-Playbook für Small Language Models sowie Notizen zur DevCon London 2026. Der Rohbestand wuchs von 213 auf 234 Artikel (plus 25 statt 24 PDFs), das Wiki von 334 auf 380 Konzeptseiten (Entities 107→115, Paper-Seiten 15→16).
- **Index:** Angepasst — manueller Eintrag auf 2.220 Dateien/546M und die neuen Zahlen aktualisiert, Zusammenfassung um den neuen Ausbau ergänzt.
