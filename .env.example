# Keine echten Secrets eintragen.
EXAMPLE_API_KEY=

# X (Twitter) API — App-only Bearer Token für scripts/ingest-x.mjs
X_BEARER_TOKEN=

# Apify — API-Token für scripts/tiktok-transcript.mjs
# Console → Settings → API & Integrations → Personal API token
APIFY_TOKEN=
